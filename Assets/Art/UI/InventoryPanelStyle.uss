/* =================================================================
   SUNOD — Skill Equip Screen
   Navy + gold palette — matches MainMenu. Dark base, gold accents,
   white text. Pixel-readable throughout.
   ================================================================= */

/* ---------- BACKDROP ---------- */
.overlay {
    position: absolute;
    top: 0; left: 0; right: 0; bottom: 0;
    background-color: rgba(4, 8, 20, 0.80);
    align-items: center;
    justify-content: center;
}

/* ---------- OUTER FRAME ---------- */
.panel-root {
    width: 96%;
    max-width: 760px;
    height: 820px;
    background-color: rgb(10, 14, 30);
    border-color: rgb(201, 151, 59);
    border-width: 3px;
    padding: 3px;
}

/* ---------- INNER WELL ---------- */
.panel-inner {
    flex-grow: 1;
    flex-direction: column;
    background-color: rgb(14, 20, 42);
    border-color: rgb(30, 42, 80);
    border-width: 2px;
    padding: 8px;
}

/* ---------- TOP BAR ---------- */
.topbar {
    flex-direction: row;
    align-items: center;
    height: 50px;
    flex-shrink: 0;
    background-color: rgb(18, 26, 56);
    border-color: rgb(201, 151, 59);
    border-width: 2px;
    border-radius: 6px;
    padding: 0 10px;
    margin-bottom: 7px;
}

.btn-back {
    background-color: rgb(38, 54, 88);
    border-color: rgb(201, 151, 59);
    border-width: 2px;
    border-radius: 6px;
    padding: 8px 14px;
    min-width: 70px;
    transition-property: background-color;
    transition-duration: 0.12s;
}
.btn-back:hover { background-color: rgb(52, 72, 112); }
.btn-back:active { background-color: rgb(26, 38, 68); }
.btn-back-label {
    -unity-font-definition: url("../Fonts/Silkscreen/Silkscreen-Bold.ttf");
    font-size: 12px;
    color: rgb(255, 255, 255);
    -unity-text-align: middle-center;
    letter-spacing: 1px;
}

.title-block {
    flex-grow: 1;
    align-items: center;
    justify-content: center;
}
.panel-title {
    -unity-font-definition: url("../Fonts/Silkscreen/Silkscreen-Bold.ttf");
    font-size: 16px;
    color: rgb(245, 168, 35);
    -unity-text-align: middle-center;
    letter-spacing: 5px;
}
.panel-sub {
    -unity-font-definition: url("../Fonts/Silkscreen/Silkscreen-Regular.ttf");
    font-size: 10px;
    color: rgb(140, 110, 58);
    -unity-text-align: middle-center;
    margin-top: 1px;
}

.btn-cheat {
    background-color: rgb(38, 54, 88);
    border-color: rgb(201, 151, 59);
    border-width: 2px;
    border-radius: 6px;
    padding: 8px 12px;
    min-width: 70px;
    transition-property: background-color;
    transition-duration: 0.12s;
}
.btn-cheat:hover { background-color: rgb(52, 72, 112); }
.btn-cheat:active { background-color: rgb(26, 38, 68); }
.btn-cheat-label {
    -unity-font-definition: url("../Fonts/Silkscreen/Silkscreen-Bold.ttf");
    font-size: 10px;
    color: rgb(245, 168, 35);
    -unity-text-align: middle-center;
}

/* ---------- SECTION DIVIDER ---------- */
.section-divider {
    height: 2px;
    background-color: rgb(201, 151, 59);
    opacity: 0.35;
    margin-top: 0;
    margin-bottom: 0;
    flex-shrink: 0;
}

/* ---------- SKILL GRID BODY ---------- */
.body {
    flex-grow: 1;
    flex-direction: column;
    min-height: 220px;
}

.pane-header {
    -unity-font-definition: url("../Fonts/Silkscreen/Silkscreen-Bold.ttf");
    font-size: 10px;
    color: rgb(245, 168, 35);
    background-color: rgb(18, 26, 56);
    -unity-text-align: middle-left;
    padding: 6px 10px;
    letter-spacing: 3px;
    border-left-color: rgb(201, 151, 59);
    border-left-width: 3px;
    border-bottom-color: rgb(30, 42, 80);
    border-bottom-width: 2px;
    flex-shrink: 0;
}

.cards-scroll {
    flex-grow: 1;
    padding: 4px;
    background-color: rgb(12, 18, 38);
}

/* Force flex-wrap — descendant selector, content container is nested deep */
.cards-scroll #unity-content-container,
.cards-scroll .unity-scroll-view__content-container {
    flex-wrap: wrap;
    flex-direction: row;
    padding: 2px;
    align-content: flex-start;
}

/* ---------- SKILL CARD (grid item, 4 per row) ---------- */
.skill-row {
    width: 130px;
    margin: 4px;
    height: 100px;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start;
    background-color: rgb(22, 32, 64);
    border-color: rgb(38, 54, 96);
    border-width: 2px;
    border-radius: 4px;
    padding: 6px 4px 4px 4px;
    transition-property: background-color;
    transition-duration: 0.10s;
}
.skill-row:hover {
    background-color: rgb(32, 46, 84);
}
.skill-row.selected {
    background-color: rgb(30, 44, 78);
    border-color: rgb(201, 151, 59);
    border-width: 2px;
}

.row-icon-frame {
    width: 56px;
    height: 56px;
    background-color: rgb(10, 14, 30);
    border-color: rgb(38, 54, 96);
    border-width: 2px;
    border-radius: 3px;
    margin-bottom: 4px;
}
.row-icon {
    position: absolute;
    top: 2px; left: 2px; right: 2px; bottom: 2px;
    -unity-background-scale-mode: scale-to-fit;
}

.row-textblock {
    align-items: center;
    flex-direction: column;
    justify-content: center;
}
.row-name {
    -unity-font-definition: url("../Fonts/Silkscreen/Silkscreen-Bold.ttf");
    font-size: 9px;
    color: rgb(210, 210, 230);
    -unity-text-align: middle-center;
    white-space: normal;
    overflow: hidden;
}
.row-meta {
    display: none;
}

/* RIASEC corner badge */
.row-tag {
    width: 18px;
    height: 18px;
    align-items: center;
    justify-content: center;
    position: absolute;
    top: 2px;
    right: 2px;
    border-radius: 2px;
}
.row-tag-letter {
    -unity-font-definition: url("../Fonts/Silkscreen/Silkscreen-Bold.ttf");
    font-size: 9px;
    color: white;
    -unity-text-align: middle-center;
}

/* RIASEC — vivid against dark navy */
.riasec-r { background-color: rgb(200, 50, 38); }
.riasec-i { background-color: rgb(40, 100, 200); }
.riasec-a { background-color: rgb(140, 50, 200); }
.riasec-s { background-color: rgb(38, 160, 56); }
.riasec-e { background-color: rgb(200, 158, 20); }
.riasec-c { background-color: rgb(28, 165, 165); }

/* ---------- DETAIL PANE (full-width bottom) ---------- */
.detail-pane {
    background-color: rgb(18, 26, 56);
    border-color: rgb(201, 151, 59);
    border-width: 2px;
    border-radius: 4px;
    padding: 10px 14px 10px 14px;
    height: 175px;
    flex-shrink: 0;
    flex-direction: column;
}

/* Empty state */
.detail-empty {
    flex-grow: 1;
    align-items: center;
    justify-content: center;
    flex-direction: row;
}
.detail-empty-title {
    -unity-font-definition: url("../Fonts/Silkscreen/Silkscreen-Bold.ttf");
    font-size: 12px;
    color: rgb(80, 96, 140);
    letter-spacing: 3px;
    margin-right: 12px;
}
.detail-empty-sub {
    -unity-font-definition: url("../Fonts/Silkscreen/Silkscreen-Regular.ttf");
    font-size: 11px;
    color: rgb(60, 76, 112);
}

/* Filled state */
.detail-filled {
    flex-grow: 1;
    flex-direction: column;
}

.detail-header {
    flex-direction: row;
    align-items: center;
    margin-bottom: 6px;
}

.detail-icon-frame {
    width: 56px;
    height: 56px;
    background-color: rgb(10, 14, 30);
    border-color: rgb(201, 151, 59);
    border-width: 2px;
    border-radius: 4px;
    margin-right: 12px;
    flex-shrink: 0;
}
.detail-icon {
    position: absolute;
    top: 3px; left: 3px; right: 3px; bottom: 3px;
    -unity-background-scale-mode: scale-to-fit;
}

.detail-titlebox {
    flex-grow: 1;
    flex-direction: column;
    justify-content: center;
}
.detail-name {
    -unity-font-definition: url("../Fonts/Silkscreen/Silkscreen-Bold.ttf");
    font-size: 13px;
    color: rgb(255, 255, 255);
    letter-spacing: 2px;
    white-space: normal;
    margin-bottom: 4px;
}
.detail-meta {
    flex-direction: row;
    align-items: center;
}
.detail-tag {
    flex-direction: row;
    align-items: center;
    padding: 2px 8px 2px 5px;
    margin-right: 8px;
    border-radius: 3px;
}
.detail-tag-letter {
    -unity-font-definition: url("../Fonts/Silkscreen/Silkscreen-Bold.ttf");
    font-size: 12px;
    color: white;
    margin-right: 4px;
}
.detail-tag-name {
    -unity-font-definition: url("../Fonts/Silkscreen/Silkscreen-Bold.ttf");
    font-size: 8px;
    color: white;
    letter-spacing: 1px;
}
.detail-level {
    -unity-font-definition: url("../Fonts/Silkscreen/Silkscreen-Regular.ttf");
    font-size: 11px;
    color: rgb(245, 168, 35);
    background-color: rgb(10, 14, 30);
    border-color: rgb(201, 151, 59);
    border-width: 2px;
    border-radius: 3px;
    padding: 2px 8px;
}

.detail-divider {
    height: 1px;
    background-color: rgb(201, 151, 59);
    opacity: 0.4;
    margin-bottom: 8px;
}

/* Two-column row for description + action */
.detail-body-row {
    flex-direction: row;
    flex-grow: 1;
    margin-bottom: 6px;
}
.detail-desc-block {
    flex-grow: 1;
    flex-direction: column;
    margin-right: 10px;
}
.detail-action-block {
    flex-grow: 1;
    flex-direction: column;
}

.detail-section-label {
    -unity-font-definition: url("../Fonts/Silkscreen/Silkscreen-Bold.ttf");
    font-size: 8px;
    color: rgb(140, 110, 58);
    letter-spacing: 3px;
    margin-bottom: 3px;
}
.detail-desc {
    -unity-font-definition: url("../Fonts/Silkscreen/Silkscreen-Regular.ttf");
    font-size: 10px;
    color: rgb(210, 210, 230);
    white-space: normal;
    overflow: hidden;
}
.detail-action {
    -unity-font-definition: url("../Fonts/Silkscreen/Silkscreen-Regular.ttf");
    font-size: 10px;
    color: rgb(210, 210, 230);
    background-color: rgb(12, 18, 38);
    border-left-color: rgb(245, 168, 35);
    border-left-width: 3px;
    border-top-color: rgb(30, 42, 80);
    border-top-width: 2px;
    border-right-color: rgb(30, 42, 80);
    border-right-width: 2px;
    border-bottom-color: rgb(30, 42, 80);
    border-bottom-width: 2px;
    padding: 4px 8px;
    white-space: normal;
    overflow: hidden;
    flex-grow: 1;
}

.detail-hint {
    -unity-font-definition: url("../Fonts/Silkscreen/Silkscreen-Bold.ttf");
    font-size: 9px;
    color: rgb(245, 168, 35);
    -unity-text-align: middle-center;
    letter-spacing: 3px;
    border-top-color: rgb(30, 42, 80);
    border-top-width: 2px;
    padding-top: 6px;
    opacity: 0.85;
}

/* ---------- EQUIPPED SECTION (top) ---------- */
.equipped-section {
    background-color: rgb(18, 26, 56);
    border-color: rgb(201, 151, 59);
    border-width: 2px;
    border-radius: 4px;
    padding: 8px 12px 14px 12px;
    align-items: center;
    margin-bottom: 0;
    flex-shrink: 0;
}

.equipped-title {
    -unity-font-definition: url("../Fonts/Silkscreen/Silkscreen-Bold.ttf");
    font-size: 10px;
    color: rgb(140, 110, 58);
    -unity-text-align: middle-center;
    letter-spacing: 5px;
    margin-bottom: 8px;
}

.slots-row {
    flex-direction: row;
    justify-content: center;
    align-items: flex-start;
}

/* Wrapper: column — number badge, diamond container, skill name label */
.slot-wrapper {
    align-items: center;
    justify-content: flex-start;
    flex-direction: column;
    width: 88px;
    margin-left: 10px;
    margin-right: 10px;
}

/* Number badge */
.slot-number {
    -unity-font-definition: url("../Fonts/Silkscreen/Silkscreen-Bold.ttf");
    font-size: 10px;
    color: rgb(245, 168, 35);
    -unity-text-align: middle-center;
    margin-bottom: 5px;
    background-color: rgb(10, 14, 30);
    border-color: rgb(201, 151, 59);
    border-width: 2px;
    border-radius: 3px;
    padding: 2px 8px;
    letter-spacing: 1px;
}

/* Container sized to contain the rotated diamond — 54×√2 ≈ 76px, padded to 80px */
.slot-diamond-container {
    width: 80px;
    height: 80px;
    align-items: center;
    justify-content: center;
}

/* Diamond: 54×54 rotated 45° fits inside 80×80 container */
.slot-frame {
    width: 54px;
    height: 54px;
    rotate: 45deg;
    background-color: rgb(22, 32, 64);
    border-color: rgb(38, 54, 96);
    border-width: 3px;
    border-radius: 3px;
    padding: 0;
    align-items: center;
    justify-content: center;
    transition-property: background-color;
    transition-duration: 0.10s;
}
.slot-frame:hover {
    background-color: rgb(32, 46, 84);
    border-color: rgb(201, 151, 59);
}
.slot-frame:active {
    background-color: rgb(14, 20, 46);
}
.slot-frame.ready {
    background-color: rgb(28, 40, 72);
    border-color: rgb(201, 151, 59);
}

/* Counter-rotate inner content so icon/label stay upright */
.slot-inner {
    rotate: -45deg;
    position: absolute;
    top: 0; left: 0; right: 0; bottom: 0;
    align-items: center;
    justify-content: center;
}

.slot-frame-icon {
    position: absolute;
    top: 4px; left: 4px; right: 4px; bottom: 4px;
    -unity-background-scale-mode: scale-to-fit;
}
.slot-empty-label {
    -unity-font-definition: url("../Fonts/Silkscreen/Silkscreen-Bold.ttf");
    font-size: 16px;
    color: rgb(50, 68, 110);
    -unity-text-align: middle-center;
}

/* Skill name below the diamond — "——" when empty, skill name when equipped */
.slot-name-label {
    -unity-font-definition: url("../Fonts/Silkscreen/Silkscreen-Bold.ttf");
    font-size: 9px;
    color: rgb(245, 168, 35);
    -unity-text-align: middle-center;
    margin-top: 4px;
    white-space: nowrap;
    overflow: hidden;
    width: 88px;
}
